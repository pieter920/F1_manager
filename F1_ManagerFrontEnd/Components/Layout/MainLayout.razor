@inherits LayoutComponentBase
@inject NavigationManager NavManager
@inject UserStateService UserState
@inject NavigationManager Navigation

<div class="page">
    @if (!NavManager.Uri.Contains("/login") && !NavManager.Uri.EndsWith("/")
                && !NavManager.Uri.EndsWith("/register") && !NavManager.Uri.EndsWith("/CreateTeam") 
                    && !NavManager.Uri.EndsWith("/CreateDriver") && !NavManager.Uri.EndsWith("/CreateDriver2"))
    {
        <div class="sidebar">
            <NavMenu />
        </div>
    }

    <main>
    <div class="top-row px-4">
            @if (!NavManager.Uri.Contains("/login") && !NavManager.Uri.EndsWith("/")
                            && !NavManager.Uri.EndsWith("/register") && !NavManager.Uri.EndsWith("/CreateTeam") 
                                        && !NavManager.Uri.EndsWith("/CreateDriver") && !NavManager.Uri.EndsWith("/CreateDriver2"))
            {
                <F1_ManagerFrontEnd.Components.UserDropdown />
            }

    </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>